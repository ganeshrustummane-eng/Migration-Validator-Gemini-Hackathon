# ============================================================
# Migration Validator — Data Validation Config
# Table      : migration_test
# Generated  : 2026-08-29T12:10:32.957996
# By         : ai (model: gpt-4o)
# Columns    : 20 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
#   - Fivetran  : WHERE _FIVETRAN_ACTIVE = TRUE (Snowflake side — active records only)
# ============================================================

tables:
  migration_test:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: migration_test
        source: postgresql
        pksourcecolumn: row_id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(row_id AS TEXT), '<<NULL>>') AS row_id_normalized,
            COALESCE(CAST(col_varchar AS TEXT), '<<NULL>>') AS col_varchar_normalized,
            COALESCE(CAST(col_text AS TEXT), '<<NULL>>') AS col_text_normalized,
            COALESCE(CAST(col_char AS TEXT), '<<NULL>>') AS col_char_normalized,
            COALESCE(CASE WHEN col_boolean = true THEN '1' WHEN col_boolean = false THEN '0' ELSE NULL END, '<<NULL>>') AS col_boolean_normalized,
            COALESCE(CAST(col_smallint AS TEXT), '<<NULL>>') AS col_smallint_normalized,
            COALESCE(CAST(col_integer AS TEXT), '<<NULL>>') AS col_integer_normalized,
            COALESCE(CAST(col_bigint AS TEXT), '<<NULL>>') AS col_bigint_normalized,
            COALESCE(CAST(col_numeric AS TEXT), '<<NULL>>') AS col_numeric_normalized,
            COALESCE(CAST(col_decimal AS TEXT), '<<NULL>>') AS col_decimal_normalized,
            COALESCE(CAST(col_float AS TEXT), '<<NULL>>') AS col_float_normalized,
            COALESCE(CAST(col_date AS TEXT), '<<NULL>>') AS col_date_normalized,
            COALESCE(TO_CHAR(col_timestamp_ntz, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS col_timestamp_ntz_normalized,
            COALESCE(TO_CHAR(col_timestamp_tz, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS col_timestamp_tz_normalized,
            COALESCE(CAST(col_uuid AS TEXT), '<<NULL>>') AS col_uuid_normalized,
            COALESCE(CAST(col_json AS TEXT), '<<NULL>>') AS col_json_normalized,
            COALESCE(CAST(col_jsonb AS TEXT), '<<NULL>>') AS col_jsonb_normalized,
            COALESCE(CAST(col_bytea AS TEXT), '<<NULL>>') AS col_bytea_normalized,
            COALESCE(CAST(col_hstore AS TEXT), '<<NULL>>') AS col_hstore_normalized,
            COALESCE(CAST(test_label AS TEXT), '<<NULL>>') AS test_label_normalized
          FROM public.migration_test;
        target_table_name: MIGRATION_TEST
        target: snowflake
        pktargetcolumn: ROW_ID_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
              COALESCE(CAST(ROW_ID AS STRING), '<<NULL>>') AS "row_id_normalized",
              COALESCE(CAST(COL_VARCHAR AS STRING), '<<NULL>>') AS "col_varchar_normalized",
              COALESCE(CAST(COL_TEXT AS STRING), '<<NULL>>') AS "col_text_normalized",
              COALESCE(CAST(COL_CHAR AS STRING), '<<NULL>>') AS "col_char_normalized",
              COALESCE(CASE WHEN COL_BOOLEAN = TRUE THEN '1' WHEN COL_BOOLEAN = FALSE THEN '0' ELSE NULL END, '<<NULL>>') AS "col_boolean_normalized",
              COALESCE(CAST(COL_SMALLINT AS STRING), '<<NULL>>') AS "col_smallint_normalized",
              COALESCE(CAST(COL_INTEGER AS STRING), '<<NULL>>') AS "col_integer_normalized",
              COALESCE(CAST(COL_BIGINT AS STRING), '<<NULL>>') AS "col_bigint_normalized",
              COALESCE(CAST(COL_NUMERIC AS STRING), '<<NULL>>') AS "col_numeric_normalized",
              COALESCE(CAST(COL_DECIMAL AS STRING), '<<NULL>>') AS "col_decimal_normalized",
              COALESCE(CAST(COL_FLOAT AS STRING), '<<NULL>>') AS "col_float_normalized",
              COALESCE(TO_VARCHAR(COL_DATE, 'YYYY-MM-DD'), '<<NULL>>') AS "col_date_normalized",
              COALESCE(TO_VARCHAR(COL_TIMESTAMP_NTZ, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "col_timestamp_ntz_normalized",
              COALESCE(TO_VARCHAR(COL_TIMESTAMP_TZ, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "col_timestamp_tz_normalized",
              COALESCE(CAST(COL_UUID AS STRING), '<<NULL>>') AS "col_uuid_normalized",
              COALESCE(CAST(COL_JSON AS STRING), '<<NULL>>') AS "col_json_normalized",
              COALESCE(CAST(COL_JSONB AS STRING), '<<NULL>>') AS "col_jsonb_normalized",
              COALESCE(CAST(COL_BYTEA AS STRING), '<<NULL>>') AS "col_bytea_normalized",
              COALESCE(CAST(COL_HSTORE AS STRING), '<<NULL>>') AS "col_hstore_normalized",
              COALESCE(CAST(TEST_LABEL AS STRING), '<<NULL>>') AS "test_label_normalized"
          FROM dev_edge_bronze.storedge_fms_public.MIGRATION_TEST
          WHERE _FIVETRAN_ACTIVE = TRUE;
