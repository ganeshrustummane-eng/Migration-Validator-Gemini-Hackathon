# ============================================================
# Migration Validator — Data Validation Config
# Table      : orders
# Generated  : 2026-09-01T21:56:10.886645
# By         : ai (model: gpt-4o)
# Columns    : 8 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  orders:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: orders
        source: postgresql
        source_database: mv_db_test
        source_schema: mv_analytics
        pksourcecolumn: ord_id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(CAST(ord_id AS TEXT) AS TEXT), '<<NULL>>') AS ord_id_normalized,
            COALESCE(CAST(CAST(cust_id AS TEXT) AS TEXT), '<<NULL>>') AS cust_id_normalized,
            COALESCE(CAST(TO_CHAR(ord_dt, 'YYYY-MM-DD') AS TEXT), '<<NULL>>') AS ord_dt_normalized,
            COALESCE(CAST(CAST(CAST(tot_amt AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS tot_amt_normalized,
            COALESCE(CAST(TRIM(ord_sts) AS TEXT), '<<NULL>>') AS ord_sts_normalized,
            COALESCE(CAST(TRIM(shp_addr) AS TEXT), '<<NULL>>') AS shp_addr_normalized,
            COALESCE(CAST(CAST(itm_cnt AS TEXT) AS TEXT), '<<NULL>>') AS itm_cnt_normalized,
            COALESCE(CAST(CAST(CAST(disc_pct AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS disc_pct_normalized
          FROM mv_analytics.orders;
        target_table_name: ORDERS
        target: snowflake
        target_database: SNOWFLAKE_TARGET_DB
        target_schema: MV_ANALYTICS
        pktargetcolumn: ord_id_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
            COALESCE(CAST(CAST(ORDER_ID AS STRING) AS STRING), '<<NULL>>') AS "ord_id_normalized",
            COALESCE(CAST(CAST(CUSTOMER_ID AS STRING) AS STRING), '<<NULL>>') AS "cust_id_normalized",
            COALESCE(CAST(TO_VARCHAR(ORDER_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "ord_dt_normalized",
            COALESCE(CAST(CAST(TOTAL_AMOUNT AS STRING) AS STRING), '<<NULL>>') AS "tot_amt_normalized",
            COALESCE(CAST(TRIM(ORDER_STATUS) AS STRING), '<<NULL>>') AS "ord_sts_normalized",
            COALESCE(CAST(TRIM(SHIPPING_ADDRESS) AS STRING), '<<NULL>>') AS "shp_addr_normalized",
            COALESCE(CAST(CAST(ITEM_COUNT AS STRING) AS STRING), '<<NULL>>') AS "itm_cnt_normalized",
            COALESCE(CAST(CAST(DISCOUNT_PCT AS STRING) AS STRING), '<<NULL>>') AS "disc_pct_normalized"
          FROM SNOWFLAKE_TARGET_DB.MV_ANALYTICS.ORDERS;
