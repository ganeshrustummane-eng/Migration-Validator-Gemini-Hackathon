# ============================================================
# Migration Validator — Data Validation Config
# Table      : xyz
# Generated  : 2026-08-27T17:21:52.323881
# By         : ai (model: gpt-4o)
# Columns    : 3 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  xyz:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: xyz
        source: postgresql
        pksourcecolumn: created_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(created AS TEXT), '<<NULL>>') AS created_normalized,
            COALESCE(CAST(employee AS TEXT), '<<NULL>>') AS employee_normalized,
            COALESCE(CAST(salary AS TEXT), '<<NULL>>') AS salary_normalized
          FROM public.xyz;
        target_table_name: xyz
        target: snowflake
        pktargetcolumn: CREATED_AT_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
              COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS') AS STRING), '<<NULL>>') AS "created_normalized",
              COALESCE(CAST(USER AS STRING), '<<NULL>>') AS "employee_normalized",
              COALESCE(CAST(SALARY AS STRING), '<<NULL>>') AS "salary_normalized"
          FROM dev_edge_bronze.storedge_fms_public.xyz;
