# ============================================================
# Migration Validator — Data Validation Config
# Table      : customers
# Generated  : 2026-09-02T10:44:14.145807
# By         : ai (model: gpt-4o)
# Columns    : 8 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  customers:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: customers
        source: postgresql
        source_database: mv_db_test
        source_schema: mv_demo
        pksourcecolumn: cust_id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(CAST(cust_id AS TEXT) AS TEXT), '<<NULL>>') AS cust_id_normalized,
            COALESCE(CAST(TRIM(cust_nm) AS TEXT), '<<NULL>>') AS cust_nm_normalized,
            COALESCE(CAST(TRIM(cust_email) AS TEXT), '<<NULL>>') AS cust_email_normalized,
            COALESCE(CAST(TRIM(phn_no) AS TEXT), '<<NULL>>') AS phn_no_normalized,
            COALESCE(CAST(TO_CHAR(acct_crt_dt, 'YYYY-MM-DD') AS TEXT), '<<NULL>>') AS acct_crt_dt_normalized,
            COALESCE(CAST(CAST(CAST(acct_bal_amt AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS acct_bal_amt_normalized,
            COALESCE(CAST(CASE WHEN is_actv = true THEN '1' WHEN is_actv = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS is_actv_normalized,
            COALESCE(CAST(TRIM(rgn_cd) AS TEXT), '<<NULL>>') AS rgn_cd_normalized
          FROM mv_demo.customers;
        target_table_name: CUSTOMERS
        target: snowflake
        target_database: SNOWFLAKE_TARGET_DB
        target_schema: MV_DEMO
        pktargetcolumn: CUSTOMER_ID_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
            COALESCE(CAST(CAST(CUSTOMER_ID AS STRING) AS STRING), '<<NULL>>') AS "cust_id_normalized",
            COALESCE(CAST(TRIM(CUSTOMER_NAME) AS STRING), '<<NULL>>') AS "cust_nm_normalized",
            COALESCE(CAST(TRIM(EMAIL_ADDRESS) AS STRING), '<<NULL>>') AS "cust_email_normalized",
            COALESCE(CAST(TRIM(PHONE_NUMBER) AS STRING), '<<NULL>>') AS "phn_no_normalized",
            COALESCE(CAST(TO_VARCHAR(ACCOUNT_CREATED_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "acct_crt_dt_normalized",
            COALESCE(CAST(CAST(ACCOUNT_BALANCE AS STRING) AS STRING), '<<NULL>>') AS "acct_bal_amt_normalized",
            COALESCE(CAST(CASE WHEN IS_ACTIVE = TRUE THEN '1' WHEN IS_ACTIVE = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "is_actv_normalized",
            COALESCE(CAST(TRIM(REGION_CODE) AS STRING), '<<NULL>>') AS "rgn_cd_normalized"
          FROM SNOWFLAKE_TARGET_DB.MV_DEMO.CUSTOMERS;
