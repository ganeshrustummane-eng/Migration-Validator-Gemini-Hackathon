# ============================================================
# Migration Validator — Data Validation Config
# Table      : events
# Generated  : 2026-08-27T15:49:41.450298
# By         : ai (model: gpt-4o)
# Columns    : 39 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
#   - Fivetran  : WHERE _FIVETRAN_ACTIVE = TRUE (Snowflake side — active records only)
# ============================================================

tables:
  events:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: events
        source: postgresql
        pksourcecolumn: id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(id AS TEXT), '<<NULL>>') AS id_normalized,
            COALESCE(CAST(type AS TEXT), '<<NULL>>') AS type_normalized,
            COALESCE(CAST(data AS TEXT), '<<NULL>>') AS data_normalized,
            COALESCE(CASE WHEN needs_followup = true THEN '1' WHEN needs_followup = false THEN '0' ELSE NULL END, '<<NULL>>') AS needs_followup_normalized,
            COALESCE(TO_CHAR(needs_followup_by, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS needs_followup_by_normalized,
            COALESCE(TO_CHAR(followup_date, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS followup_date_normalized,
            COALESCE(CASE WHEN resolved = true THEN '1' WHEN resolved = false THEN '0' ELSE NULL END, '<<NULL>>') AS resolved_normalized,
            COALESCE(CAST(created_by_id AS TEXT), '<<NULL>>') AS created_by_id_normalized,
            COALESCE(CAST(updated_by_id AS TEXT), '<<NULL>>') AS updated_by_id_normalized,
            COALESCE(CAST(unit_id AS TEXT), '<<NULL>>') AS unit_id_normalized,
            COALESCE(CAST(tenant_id AS TEXT), '<<NULL>>') AS tenant_id_normalized,
            COALESCE(CAST(source_id AS TEXT), '<<NULL>>') AS source_id_normalized,
            COALESCE(CAST(facility_id AS TEXT), '<<NULL>>') AS facility_id_normalized,
            COALESCE(TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS created_at_normalized,
            COALESCE(TO_CHAR(updated_at, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS updated_at_normalized,
            COALESCE(CAST(parent_event_id AS TEXT), '<<NULL>>') AS parent_event_id_normalized,
            COALESCE(CAST(related_object_id AS TEXT), '<<NULL>>') AS related_object_id_normalized,
            COALESCE(CAST(related_object_type AS TEXT), '<<NULL>>') AS related_object_type_normalized,
            COALESCE(CAST(uuid AS TEXT), '<<NULL>>') AS uuid_normalized,
            COALESCE(CAST(status AS TEXT), '<<NULL>>') AS status_normalized,
            COALESCE(CASE WHEN important_task = true THEN '1' WHEN important_task = false THEN '0' ELSE NULL END, '<<NULL>>') AS important_task_normalized,
            COALESCE(TO_CHAR(resolved_on, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS resolved_on_normalized,
            COALESCE(CAST(created_by_type AS TEXT), '<<NULL>>') AS created_by_type_normalized,
            COALESCE(CAST(updated_by_type AS TEXT), '<<NULL>>') AS updated_by_type_normalized,
            COALESCE(CAST(ledger_id AS TEXT), '<<NULL>>') AS ledger_id_normalized,
            COALESCE(CAST(lead_id AS TEXT), '<<NULL>>') AS lead_id_normalized,
            COALESCE(CAST(support_case_id AS TEXT), '<<NULL>>') AS support_case_id_normalized,
            COALESCE(CAST(attachment_id AS TEXT), '<<NULL>>') AS attachment_id_normalized,
            COALESCE(CAST(action_item AS TEXT), '<<NULL>>') AS action_item_normalized,
            COALESCE(CAST(ledger_delinquency_id AS TEXT), '<<NULL>>') AS ledger_delinquency_id_normalized,
            COALESCE(CAST(company_uuid AS TEXT), '<<NULL>>') AS company_uuid_normalized,
            COALESCE(CAST(facility_uuid AS TEXT), '<<NULL>>') AS facility_uuid_normalized,
            COALESCE(CAST(tenant_uuid AS TEXT), '<<NULL>>') AS tenant_uuid_normalized,
            COALESCE(CAST(ledger_uuid AS TEXT), '<<NULL>>') AS ledger_uuid_normalized,
            COALESCE(CAST(unit_uuid AS TEXT), '<<NULL>>') AS unit_uuid_normalized,
            COALESCE(CAST(lead_uuid AS TEXT), '<<NULL>>') AS lead_uuid_normalized,
            COALESCE(CAST(related_object_uuid AS TEXT), '<<NULL>>') AS related_object_uuid_normalized,
            COALESCE(CAST(client_application_id AS TEXT), '<<NULL>>') AS client_application_id_normalized,
            COALESCE(CAST(payment_intent_id AS TEXT), '<<NULL>>') AS payment_intent_id_normalized
          FROM public.events;
        target_table_name: events
        target: snowflake
        pktargetcolumn: ID_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
              COALESCE(CAST(ID AS STRING), '<<NULL>>') AS "id_normalized",
              COALESCE(CAST(TYPE AS STRING), '<<NULL>>') AS "type_normalized",
              COALESCE(CAST(DATA AS STRING), '<<NULL>>') AS "data_normalized",
              COALESCE(CASE WHEN NEEDS_FOLLOWUP = TRUE THEN '1' WHEN NEEDS_FOLLOWUP = FALSE THEN '0' ELSE NULL END, '<<NULL>>') AS "needs_followup_normalized",
              COALESCE(TO_VARCHAR(NEEDS_FOLLOWUP_BY, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "needs_followup_by_normalized",
              COALESCE(TO_VARCHAR(FOLLOWUP_DATE, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "followup_date_normalized",
              COALESCE(CASE WHEN RESOLVED = TRUE THEN '1' WHEN RESOLVED = FALSE THEN '0' ELSE NULL END, '<<NULL>>') AS "resolved_normalized",
              COALESCE(CAST(CREATED_BY_ID AS STRING), '<<NULL>>') AS "created_by_id_normalized",
              COALESCE(CAST(UPDATED_BY_ID AS STRING), '<<NULL>>') AS "updated_by_id_normalized",
              COALESCE(CAST(UNIT_ID AS STRING), '<<NULL>>') AS "unit_id_normalized",
              COALESCE(CAST(TENANT_ID AS STRING), '<<NULL>>') AS "tenant_id_normalized",
              COALESCE(CAST(SOURCE_ID AS STRING), '<<NULL>>') AS "source_id_normalized",
              COALESCE(CAST(FACILITY_ID AS STRING), '<<NULL>>') AS "facility_id_normalized",
              COALESCE(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "created_at_normalized",
              COALESCE(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "updated_at_normalized",
              COALESCE(CAST(PARENT_EVENT_ID AS STRING), '<<NULL>>') AS "parent_event_id_normalized",
              COALESCE(CAST(RELATED_OBJECT_ID AS STRING), '<<NULL>>') AS "related_object_id_normalized",
              COALESCE(CAST(RELATED_OBJECT_TYPE AS STRING), '<<NULL>>') AS "related_object_type_normalized",
              COALESCE(CAST(UUID AS STRING), '<<NULL>>') AS "uuid_normalized",
              COALESCE(CAST(STATUS AS STRING), '<<NULL>>') AS "status_normalized",
              COALESCE(CASE WHEN IMPORTANT_TASK = TRUE THEN '1' WHEN IMPORTANT_TASK = FALSE THEN '0' ELSE NULL END, '<<NULL>>') AS "important_task_normalized",
              COALESCE(TO_VARCHAR(RESOLVED_ON, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "resolved_on_normalized",
              COALESCE(CAST(CREATED_BY_TYPE AS STRING), '<<NULL>>') AS "created_by_type_normalized",
              COALESCE(CAST(UPDATED_BY_TYPE AS STRING), '<<NULL>>') AS "updated_by_type_normalized",
              COALESCE(CAST(LEDGER_ID AS STRING), '<<NULL>>') AS "ledger_id_normalized",
              COALESCE(CAST(LEAD_ID AS STRING), '<<NULL>>') AS "lead_id_normalized",
              COALESCE(CAST(SUPPORT_CASE_ID AS STRING), '<<NULL>>') AS "support_case_id_normalized",
              COALESCE(CAST(ATTACHMENT_ID AS STRING), '<<NULL>>') AS "attachment_id_normalized",
              COALESCE(CAST(ACTION_ITEM AS STRING), '<<NULL>>') AS "action_item_normalized",
              COALESCE(CAST(LEDGER_DELINQUENCY_ID AS STRING), '<<NULL>>') AS "ledger_delinquency_id_normalized",
              COALESCE(CAST(COMPANY_UUID AS STRING), '<<NULL>>') AS "company_uuid_normalized",
              COALESCE(CAST(FACILITY_UUID AS STRING), '<<NULL>>') AS "facility_uuid_normalized",
              COALESCE(CAST(TENANT_UUID AS STRING), '<<NULL>>') AS "tenant_uuid_normalized",
              COALESCE(CAST(LEDGER_UUID AS STRING), '<<NULL>>') AS "ledger_uuid_normalized",
              COALESCE(CAST(UNIT_UUID AS STRING), '<<NULL>>') AS "unit_uuid_normalized",
              COALESCE(CAST(LEAD_UUID AS STRING), '<<NULL>>') AS "lead_uuid_normalized",
              COALESCE(CAST(RELATED_OBJECT_UUID AS STRING), '<<NULL>>') AS "related_object_uuid_normalized",
              COALESCE(CAST(CLIENT_APPLICATION_ID AS STRING), '<<NULL>>') AS "client_application_id_normalized",
              COALESCE(CAST(PAYMENT_INTENT_ID AS STRING), '<<NULL>>') AS "payment_intent_id_normalized"
          FROM dev_edge_bronze.storedge_fms_public.events
          WHERE _FIVETRAN_ACTIVE = TRUE;
