# ============================================================
# Migration Validator — Data Validation Config
# Table      : departments
# Generated  : 2026-09-01T18:40:00.127523
# By         : ai (model: gpt-4o)
# Columns    : 6 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  departments:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: departments
        source: mssql
        source_database: migration_test
        source_schema: dbo
        pksourcecolumn: dept_id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(CAST(dept_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS dept_id_normalized,
            COALESCE(CAST(LTRIM(RTRIM(dept_name)) AS VARCHAR(MAX)), '<<NULL>>') AS dept_name_normalized,
            COALESCE(CAST(LTRIM(RTRIM(location)) AS VARCHAR(MAX)), '<<NULL>>') AS location_normalized,
            COALESCE(CAST(CAST(budget AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS budget_normalized,
            COALESCE(CAST(FORMAT(created_date, 'yyyy-MM-dd') AS VARCHAR(MAX)), '<<NULL>>') AS created_date_normalized,
            COALESCE(CAST(CASE WHEN is_active = 1 THEN '1' WHEN is_active = 0 THEN '0' ELSE NULL END AS VARCHAR(MAX)), '<<NULL>>') AS is_active_normalized
          FROM dbo.departments;
        target_table_name: DEPARTMENTS
        target: snowflake
        target_database: MIGRATION_TEST
        target_schema: PUBLIC
        pktargetcolumn: dept_id_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
            COALESCE(CAST(CAST(DEPT_ID AS STRING) AS STRING), '<<NULL>>') AS "dept_id_normalized",
            COALESCE(CAST(TRIM(DEPT_NAME) AS STRING), '<<NULL>>') AS "dept_name_normalized",
            COALESCE(CAST(TRIM(LOCATION) AS STRING), '<<NULL>>') AS "location_normalized",
            COALESCE(CAST(CAST(BUDGET AS STRING) AS STRING), '<<NULL>>') AS "budget_normalized",
            COALESCE(CAST(TO_VARCHAR(CREATED_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "created_date_normalized",
            COALESCE(CAST(CASE WHEN IS_ACTIVE = TRUE THEN '1' WHEN IS_ACTIVE = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "is_active_normalized"
          FROM MIGRATION_TEST.PUBLIC.DEPARTMENTS;
