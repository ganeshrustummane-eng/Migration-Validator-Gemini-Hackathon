# ============================================================
# Migration Validator — Data Validation Config
# Table      : salaries
# Generated  : 2026-09-01T18:41:09.129318
# By         : ai (model: gpt-4o)
# Columns    : 7 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  salaries:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: salaries
        source: mssql
        source_database: migration_test
        source_schema: dbo
        pksourcecolumn:
          - employee_id_normalized
          - effective_date_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(CAST(employee_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS employee_id_normalized,
            COALESCE(CAST(FORMAT(effective_date, 'yyyy-MM-dd') AS VARCHAR(MAX)), '<<NULL>>') AS effective_date_normalized,
            COALESCE(CAST(CAST(base_salary AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS base_salary_normalized,
            COALESCE(CAST(CAST(bonus AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS bonus_normalized,
            COALESCE(CAST(LTRIM(RTRIM(currency)) AS VARCHAR(MAX)), '<<NULL>>') AS currency_normalized,
            COALESCE(CAST(LTRIM(RTRIM(pay_grade)) AS VARCHAR(MAX)), '<<NULL>>') AS pay_grade_normalized,
            COALESCE(CAST(LTRIM(RTRIM(notes)) AS VARCHAR(MAX)), '<<NULL>>') AS notes_normalized
          FROM dbo.salaries;
        target_table_name: SALARIES
        target: snowflake
        target_database: MIGRATION_TEST
        target_schema: PUBLIC
        pktargetcolumn:
          - employee_id_normalized
          - effective_date_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
            COALESCE(CAST(CAST(EMPLOYEE_ID AS STRING) AS STRING), '<<NULL>>') AS "employee_id_normalized",
            COALESCE(CAST(TO_VARCHAR(EFFECTIVE_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "effective_date_normalized",
            COALESCE(CAST(CAST(BASE_SALARY AS STRING) AS STRING), '<<NULL>>') AS "base_salary_normalized",
            COALESCE(CAST(CAST(BONUS AS STRING) AS STRING), '<<NULL>>') AS "bonus_normalized",
            COALESCE(CAST(TRIM(CURRENCY) AS STRING), '<<NULL>>') AS "currency_normalized",
            COALESCE(CAST(TRIM(PAY_GRADE) AS STRING), '<<NULL>>') AS "pay_grade_normalized",
            COALESCE(CAST(TRIM(NOTES) AS STRING), '<<NULL>>') AS "notes_normalized"
          FROM MIGRATION_TEST.PUBLIC.SALARIES;
