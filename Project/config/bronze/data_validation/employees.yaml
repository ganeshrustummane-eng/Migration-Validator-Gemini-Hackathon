# ============================================================
# Migration Validator — Data Validation Config
# Table      : employees
# Generated  : 2026-09-01T18:40:33.979365
# By         : ai (model: gpt-4o)
# Columns    : 9 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  employees:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: employees
        source: mssql
        source_database: migration_test
        source_schema: dbo
        pksourcecolumn: employee_id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(CAST(employee_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS employee_id_normalized,
            COALESCE(CAST(LTRIM(RTRIM(first_name)) AS VARCHAR(MAX)), '<<NULL>>') AS first_name_normalized,
            COALESCE(CAST(LTRIM(RTRIM(last_name)) AS VARCHAR(MAX)), '<<NULL>>') AS last_name_normalized,
            COALESCE(CAST(LTRIM(RTRIM(email)) AS VARCHAR(MAX)), '<<NULL>>') AS email_normalized,
            COALESCE(CAST(FORMAT(hire_date, 'yyyy-MM-dd') AS VARCHAR(MAX)), '<<NULL>>') AS hire_date_normalized,
            COALESCE(CAST(CAST(dept_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS dept_id_normalized,
            COALESCE(CAST(CAST(manager_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS manager_id_normalized,
            COALESCE(CAST(LTRIM(RTRIM(job_title)) AS VARCHAR(MAX)), '<<NULL>>') AS job_title_normalized,
            COALESCE(CAST(CAST(years_exp AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS years_exp_normalized
          FROM dbo.employees;
        target_table_name: EMPLOYEES
        target: snowflake
        target_database: MIGRATION_TEST
        target_schema: PUBLIC
        pktargetcolumn: employee_id_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
            COALESCE(CAST(CAST(EMPLOYEE_ID AS STRING) AS STRING), '<<NULL>>') AS "employee_id_normalized",
            COALESCE(CAST(TRIM(FIRST_NAME) AS STRING), '<<NULL>>') AS "first_name_normalized",
            COALESCE(CAST(TRIM(LAST_NAME) AS STRING), '<<NULL>>') AS "last_name_normalized",
            COALESCE(CAST(TRIM(EMAIL) AS STRING), '<<NULL>>') AS "email_normalized",
            COALESCE(CAST(TO_VARCHAR(HIRE_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "hire_date_normalized",
            COALESCE(CAST(CAST(DEPT_ID AS STRING) AS STRING), '<<NULL>>') AS "dept_id_normalized",
            COALESCE(CAST(CAST(MANAGER_ID AS STRING) AS STRING), '<<NULL>>') AS "manager_id_normalized",
            COALESCE(CAST(TRIM(JOB_TITLE) AS STRING), '<<NULL>>') AS "job_title_normalized",
            COALESCE(CAST(CAST(YEARS_EXP AS STRING) AS STRING), '<<NULL>>') AS "years_exp_normalized"
          FROM MIGRATION_TEST.PUBLIC.EMPLOYEES;
