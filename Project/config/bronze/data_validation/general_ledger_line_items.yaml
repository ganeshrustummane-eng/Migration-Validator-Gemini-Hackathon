# ============================================================
# Migration Validator — Data Validation Config
# Table      : general_ledger_line_items
# Generated  : 2026-09-04T16:45:49.563711
# By         : ai (model: gpt-4o)
# Columns    : 17 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
#   - Fivetran  : WHERE _FIVETRAN_ACTIVE = TRUE (Snowflake side — active records only)
# ============================================================

tables:
  general_ledger_line_items:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: general_ledger_line_items
        source: postgresql
        source_database: fms
        source_schema: public
        pksourcecolumn: effective_on_normalized
        source_audit_column: 
        sourcequery: |
          SELECT COALESCE(CAST(TO_CHAR(effective_on, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS effective_on_normalized, COALESCE(CAST(TRIM(description) AS TEXT), '<<NULL>>') AS description_normalized, COALESCE(CAST(CAST(CAST(amount AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS amount_normalized, COALESCE(CAST(CAST(account_balance_id AS TEXT) AS TEXT), '<<NULL>>') AS account_balance_id_normalized, COALESCE(CAST(CAST(tenant_id AS TEXT) AS TEXT), '<<NULL>>') AS tenant_id_normalized, COALESCE(CAST(CAST(ledger_id AS TEXT) AS TEXT), '<<NULL>>') AS ledger_id_normalized, COALESCE(CAST(CAST(facility_id AS TEXT) AS TEXT), '<<NULL>>') AS facility_id_normalized, COALESCE(CAST(TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS created_at_normalized, COALESCE(CAST(TO_CHAR(updated_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS updated_at_normalized, COALESCE(CAST(CASE WHEN reversal = true THEN '1' WHEN reversal = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS reversal_normalized, COALESCE(CAST(CAST(account_balance_item_id AS TEXT) AS TEXT), '<<NULL>>') AS account_balance_item_id_normalized, COALESCE(CAST(TRIM(internal_account_code) AS TEXT), '<<NULL>>') AS internal_account_code_normalized, COALESCE(CAST(CASE WHEN daily_transaction = true THEN '1' WHEN daily_transaction = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS daily_transaction_normalized, COALESCE(CAST(TRIM(transaction_type) AS TEXT), '<<NULL>>') AS transaction_type_normalized, COALESCE(CAST(CASE WHEN hidden = true THEN '1' WHEN hidden = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS hidden_normalized, COALESCE(CAST(TRIM(reference_number) AS TEXT), '<<NULL>>') AS reference_number_normalized, COALESCE(CAST(CAST(id AS TEXT) AS TEXT), '<<NULL>>') AS id_normalized FROM public.general_ledger_line_items;
        target_table_name: GENERAL_LEDGER_LINE_ITEMS
        target: snowflake
        target_database: dev_edge_bronze
        target_schema: storedge_fms_public
        pktargetcolumn: effective_on_normalized
        target_audit_column: 
        targetquery: |
          SELECT COALESCE(CAST(TO_VARCHAR(EFFECTIVE_ON, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "effective_on_normalized", COALESCE(CAST(TRIM(DESCRIPTION) AS STRING), '<<NULL>>') AS "description_normalized", COALESCE(CAST(CAST(AMOUNT AS STRING) AS STRING), '<<NULL>>') AS "amount_normalized", COALESCE(CAST(CAST(ACCOUNT_BALANCE_ID AS STRING) AS STRING), '<<NULL>>') AS "account_balance_id_normalized", COALESCE(CAST(CAST(TENANT_ID AS STRING) AS STRING), '<<NULL>>') AS "tenant_id_normalized", COALESCE(CAST(CAST(LEDGER_ID AS STRING) AS STRING), '<<NULL>>') AS "ledger_id_normalized", COALESCE(CAST(CAST(FACILITY_ID AS STRING) AS STRING), '<<NULL>>') AS "facility_id_normalized", COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "created_at_normalized", COALESCE(CAST(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "updated_at_normalized", COALESCE(CAST(CASE WHEN REVERSAL = TRUE THEN '1' WHEN REVERSAL = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "reversal_normalized", COALESCE(CAST(CAST(ACCOUNT_BALANCE_ITEM_ID AS STRING) AS STRING), '<<NULL>>') AS "account_balance_item_id_normalized", COALESCE(CAST(TRIM(INTERNAL_ACCOUNT_CODE) AS STRING), '<<NULL>>') AS "internal_account_code_normalized", COALESCE(CAST(CASE WHEN DAILY_TRANSACTION = TRUE THEN '1' WHEN DAILY_TRANSACTION = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "daily_transaction_normalized", COALESCE(CAST(TRIM(TRANSACTION_TYPE) AS STRING), '<<NULL>>') AS "transaction_type_normalized", COALESCE(CAST(CASE WHEN HIDDEN = TRUE THEN '1' WHEN HIDDEN = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "hidden_normalized", COALESCE(CAST(TRIM(REFERENCE_NUMBER) AS STRING), '<<NULL>>') AS "reference_number_normalized", COALESCE(CAST(CAST(ID AS STRING) AS STRING), '<<NULL>>') AS "id_normalized" FROM dev_edge_bronze.storedge_fms_public.GENERAL_LEDGER_LINE_ITEMS WHERE _FIVETRAN_ACTIVE = TRUE;
