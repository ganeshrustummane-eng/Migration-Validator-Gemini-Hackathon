# ============================================================
# Migration Validator — Data Validation Config
# Table      : Addresses
# Generated  : 2026-08-27T17:05:20.632959
# By         : ai (model: gpt-4o)
# Columns    : 26 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
#   - Fivetran  : WHERE _FIVETRAN_ACTIVE = TRUE (Snowflake side — active records only)
# ============================================================

tables:
  Addresses:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: Addresses
        source: mssql
        pksourcecolumn: AddressID_normalized
        source_audit_column: 
        sourcequery: |
          SELECT 
            COALESCE(CAST(AddressID AS VARCHAR(MAX)), '<<NULL>>') AS AddressID_normalized,
            COALESCE(CAST(SiteID AS VARCHAR(MAX)), '<<NULL>>') AS SiteID_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sMrMrs AS VARCHAR(MAX)))), '<<NULL>>') AS sMrMrs_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sFName AS VARCHAR(MAX)))), '<<NULL>>') AS sFName_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sMI AS VARCHAR(MAX)))), '<<NULL>>') AS sMI_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sLName AS VARCHAR(MAX)))), '<<NULL>>') AS sLName_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sCompany AS VARCHAR(MAX)))), '<<NULL>>') AS sCompany_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sAddr1 AS VARCHAR(MAX)))), '<<NULL>>') AS sAddr1_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sAddr2 AS VARCHAR(MAX)))), '<<NULL>>') AS sAddr2_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sCity AS VARCHAR(MAX)))), '<<NULL>>') AS sCity_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sRegion AS VARCHAR(MAX)))), '<<NULL>>') AS sRegion_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPostalCode AS VARCHAR(MAX)))), '<<NULL>>') AS sPostalCode_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sCountry AS VARCHAR(MAX)))), '<<NULL>>') AS sCountry_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone1 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone1_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone2 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone2_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone3 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone3_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone4 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone4_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone5 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone5_normalized,
            COALESCE(LTRIM(RTRIM(CAST(sPhone6 AS VARCHAR(MAX)))), '<<NULL>>') AS sPhone6_normalized,
            COALESCE(CAST(dcLongitude AS VARCHAR(MAX)), '<<NULL>>') AS dcLongitude_normalized,
            COALESCE(CAST(dcLatitude AS VARCHAR(MAX)), '<<NULL>>') AS dcLatitude_normalized,
            COALESCE(CASE WHEN bPermanent = 1 THEN '1' WHEN bPermanent = 0 THEN '0' ELSE NULL END, '<<NULL>>') AS bPermanent_normalized,
            COALESCE(FORMAT(dDeleted, 'yyyy-MM-dd HH:mm:ss'), '<<NULL>>') AS dDeleted_normalized,
            COALESCE(FORMAT(dUpdated, 'yyyy-MM-dd HH:mm:ss'), '<<NULL>>') AS dUpdated_normalized,
            COALESCE(FORMAT(dArchived, 'yyyy-MM-dd HH:mm:ss'), '<<NULL>>') AS dArchived_normalized,
            COALESCE(CAST(OldPK AS VARCHAR(MAX)), '<<NULL>>') AS OldPK_normalized
          FROM dbo.Addresses;
        target_table_name: ADDRESSES
        target: snowflake
        pktargetcolumn: ADDRESSID_normalized
        target_audit_column: 
        targetquery: |
          SELECT 
              COALESCE(CAST(ADDRESSID AS STRING), '<<NULL>>') AS "AddressID_normalized",
              COALESCE(CAST(SITEID AS STRING), '<<NULL>>') AS "SiteID_normalized",
              COALESCE(CAST(SMRMRS AS STRING), '<<NULL>>') AS "sMrMrs_normalized",
              COALESCE(CAST(SFNAME AS STRING), '<<NULL>>') AS "sFName_normalized",
              COALESCE(CAST(SMI AS STRING), '<<NULL>>') AS "sMI_normalized",
              COALESCE(CAST(SLNAME AS STRING), '<<NULL>>') AS "sLName_normalized",
              COALESCE(CAST(SCOMPANY AS STRING), '<<NULL>>') AS "sCompany_normalized",
              COALESCE(CAST(SADDR1 AS STRING), '<<NULL>>') AS "sAddr1_normalized",
              COALESCE(CAST(SADDR2 AS STRING), '<<NULL>>') AS "sAddr2_normalized",
              COALESCE(CAST(SCITY AS STRING), '<<NULL>>') AS "sCity_normalized",
              COALESCE(CAST(SREGION AS STRING), '<<NULL>>') AS "sRegion_normalized",
              COALESCE(CAST(SPOSTALCODE AS STRING), '<<NULL>>') AS "sPostalCode_normalized",
              COALESCE(CAST(SCOUNTRY AS STRING), '<<NULL>>') AS "sCountry_normalized",
              COALESCE(CAST(SPHONE1 AS STRING), '<<NULL>>') AS "sPhone1_normalized",
              COALESCE(CAST(SPHONE2 AS STRING), '<<NULL>>') AS "sPhone2_normalized",
              COALESCE(CAST(SPHONE3 AS STRING), '<<NULL>>') AS "sPhone3_normalized",
              COALESCE(CAST(SPHONE4 AS STRING), '<<NULL>>') AS "sPhone4_normalized",
              COALESCE(CAST(SPHONE5 AS STRING), '<<NULL>>') AS "sPhone5_normalized",
              COALESCE(CAST(SPHONE6 AS STRING), '<<NULL>>') AS "sPhone6_normalized",
              COALESCE(CAST(DCLONGITUDE AS STRING), '<<NULL>>') AS "dcLongitude_normalized",
              COALESCE(CAST(DCLATITUDE AS STRING), '<<NULL>>') AS "dcLatitude_normalized",
              COALESCE(
                  CASE 
                      WHEN BPERMANENT = TRUE THEN '1' 
                      WHEN BPERMANENT = FALSE THEN '0' 
                      ELSE NULL 
                  END, 
                  '<<NULL>>'
              ) AS "bPermanent_normalized",
              COALESCE(TO_VARCHAR(DDELETED, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "dDeleted_normalized",
              COALESCE(TO_VARCHAR(DUPDATED, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "dUpdated_normalized",
              COALESCE(TO_VARCHAR(DARCHIVED, 'YYYY-MM-DD HH24:MI:SS'), '<<NULL>>') AS "dArchived_normalized",
              COALESCE(CAST(OLDPK AS STRING), '<<NULL>>') AS "OldPK_normalized"
          FROM DEV_SITELINK_BRONZE.AWSDEV_DEVT5000_DBO.ADDRESSES
          WHERE _FIVETRAN_ACTIVE = TRUE;
